apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: microservices
  labels:
    app: worker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
      - name: worker
        # In production CI/CD, the GitOps sync job updates this tag automatically:
        image: ghcr.io/amreetpoudel/worker:latest
        imagePullPolicy: IfNotPresent
        envFrom:
        - configMapRef:
            name: microservices-config
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: microservices-secrets
              key: db-password
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
